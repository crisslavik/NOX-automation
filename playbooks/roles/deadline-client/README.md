# deadline-client role

Skeleton role for Deadline client. This role expects the Deadline client installer to be placed
on the target host (see `defaults/main.yml`). It's currently a skeleton—I'll add guarded
installation steps when you confirm exact installer names/paths.
