nox-workstation-automation/
├── files/
│   ├── rpms/                # RPMs if you choose to keep them locally (not recommended)
│   ├── wallpapers/          # NOX branding assets
│   └── scripts/             # Utility scripts
├── playbooks/
│   ├── site.yml             # Role-based entrypoint for the whole site
│   ├── roles/               # Ansible roles (e.g. nice-dcv, xstudio, brave...)
│   └── softwares/           # Thin wrappers that import roles for compatibility

# Policy: installer blobs (.rpm, .run, .zip) are not stored in this repo.
# Place proprietary installers on the target hosts under /tmp or configure
# role defaults to point to the installer location before running a role.
